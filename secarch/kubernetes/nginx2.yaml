apiVersion: v1
kind: Service
metadata:
    name: 'nginx2'
    labels:
        app: 'nginx2'
spec:
    type: 'ClusterIP'
    selector:
        app: 'nginx2'
    ports:
        - port: 8880
          targetPort: 88
---
apiVersion: v1
kind: Secret
metadata:
    name: 'nginx2'
    labels:
        app: 'nginx2'
type: Opaque
data:
    WELCOME: V0VMQ09NRSE=
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: 'nginx2'
    labels:
        app: 'nginx2'
spec:
    replicas: 1
    selector:
        matchLabels:
            app: 'nginx2'
    template:
        metadata:
            labels:
                app: 'nginx2'
        spec:
            restartPolicy: Always
            containers:
                - name: 'nginx2'
                  image: 'nginx'
                  envFrom:
                    - secretRef:
                            name: 'nginx2'
                  resources:
                      limits:
                          memory: '128Mi'
                          cpu: '250m'
                  ports:
                      - containerPort: 88
